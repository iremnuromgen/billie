class AppSizes {
  // 🔹 Küçük aralıklar (ikon, küçük paddingler)
  static const double xs = 4.0;
  static const double sm = 8.0;

  // 🔹 Orta aralıklar (default paddingler, kart içi boşluklar)
  static const double md = 16.0;
  static const double lg = 24.0;

  // 🔹 Büyük aralıklar (ekran arası boşluklar, section spacing)
  static const double xl = 32.0;
  static const double xxl = 48.0;

  // 🔹 Ekstra büyük (hero görsel, onboarding spacing)
  static const double xxxl = 64.0;

  // 🔹 Yatay padding için hazır değerler
  static const double paddingHorizontal = md; // 16px
  static const double paddingHorizontalLg = lg; // 24px
}